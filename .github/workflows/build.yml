name: Build arttra.art

on:
  push:
    paths:
      - 'gallery-source/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install Pillow vtracer

      - name: Pull latest before build
        run: git pull origin main

      - name: Run pipeline
        working-directory: arttra
        run: python3 build_from_folder.py --source ../gallery-source --output ..

      - name: Commit and push
        run: |
          git config user.name "arttra-bot"
          git config user.email "bot@arttra.art"
          rm -rf assets/images/gallery/print assets/images/gallery/vector
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update: $(date -u '+%Y-%m-%d %H:%M UTC')"
            git push origin main --force
          fi
